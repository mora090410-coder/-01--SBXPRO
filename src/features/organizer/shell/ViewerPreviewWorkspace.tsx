import React from 'react';
export default function ViewerPreviewWorkspace({ children, canPublish, onReviewPublish }: { children?: React.ReactNode; canPublish: boolean; onReviewPublish: () => void }) { return <section role="region" aria-label="Viewer preview workspace" className="grid gap-3 border border-ink p-4"><h2 className="text-2xl font-semibold">Private viewer preview</h2><p className="font-semibold text-cardinal">Private preview — sharing is off</p><div className="max-w-full overflow-auto overscroll-contain">{children}</div><button type="button" className="oa-btn oa-btn-primary" disabled={!canPublish} onClick={onReviewPublish}>Review and publish</button></section>; }
